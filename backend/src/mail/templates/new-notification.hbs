<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Nueva noticia - Swokowsky Airlines</title>
    <style>
      /* Estilos mínimos inline-friendly */
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; margin:0; padding:0; background:#f6f7fb; color:#0e254d; }
      .container { max-width:700px; margin:24px auto; background:#ffffff; border-radius:12px; overflow:hidden; box-shadow:0 6px 24px rgba(16,24,40,0.08); }
      .hero { height:240px; background-size:cover; background-position:center; }
      .content { padding:24px; }
      h1 { margin:0 0 8px; font-size:22px; color:#081225; }
      p.lead { margin:0 0 12px; color:#374151; }
      .meta { font-size:13px; color:#6b7280; margin-bottom:16px; }
      .prices { display:flex; gap:12px; margin:16px 0; }
      .price { background:#f3f4f6; padding:10px 12px; border-radius:8px; font-weight:600; }
      .btn { display:inline-block; margin-top:12px; padding:10px 16px; background:#0e254d; color:white; text-decoration:none; border-radius:8px; }
      footer { font-size:12px; color:#9ca3af; text-align:center; padding:16px; }
    </style>
  </head>
  <body>
    <div class="container" role="article" aria-label="Nueva noticia">
      <!-- Banner -->
      <div class="hero" style="background-image: url('{{bannerImageUrl}}');"></div>

      <div class="content">
        <h1>{{titulo}}</h1>
        <p class="lead">{{descripcion_corta}}</p>

        <p>{{descripcion_larga}}</p>

        <div class="meta">
          <div>Salida: <strong>{{fecha_hora_salida}}</strong></div>
          <div>Llegada: <strong>{{fecha_hora_llegada}}</strong></div>
          <div>Estado: <strong>{{estado}}</strong></div>
        </div>

        <div class="prices">
          <div class="price">Económica: {{precio_economica}}</div>
          <div class="price">Primera Clase: {{precio_primera}}</div>
        </div>

        {{#if promocion}}
          <div style="margin-top:8px; padding:12px; border-radius:8px; background:#ecfdf5; color:#065f46;">
            <strong>Promoción: {{promocion.nombre}} — {{promocion.descuento}}% OFF</strong><br/>
            {{promocion.descripcion}} — <small>Válida: {{promocion.periodo_lectura}}</small>
          </div>
        {{/if}}

        <a class="btn" href="{{frontendUrl}}noticias/vuelo/{{newsId}}">Ver detalle en Swokowsky</a>

        <hr style="margin:18px 0; border:none; border-top:1px solid #eef2ff;" />

        <footer>
          Si no deseas recibir estos correos, puedes <a href="{{frontendUrl}}perfil">ingresar a la pestaña de "Información de contacto" y desuscribirte</a>.
        </footer>
      </div
    </div>
  </body>
</html>
